import React from 'react';
import './style.css'

const Resume = () => {
    return (
        <main className="resume">
            Resume
            <button>Print</button>
            <button>Download</button>
        </main>
    );
}
 
export default Resume;